# Zod Schemas

Validation schemas.

## Rules

- Never suffix class names with `Schemas`.
- Sub-domain schemas attach as `static` references on parent class.
- Never create `z.object({})` for empty objects.
